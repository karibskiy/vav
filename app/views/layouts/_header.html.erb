<div class="navbar navbar-default navbar-fixed-top navbar-inverse" role="navigation">
      <div class="navbar-inner">
        <div class="container">
          
          <div class="nav-collapse">
            <ul class="nav navbar-nav navbar-left">
              <li><%= link_to "Главная", root_url%></li>
            </ul>
            <ul class="nav navbar-nav">
              
              <% if @loggedin %>
              
              <% else %>
                <% if !signed_in? %>
                  <li><%= link_to "Регистрация", new_user_path %></li>
                  <li><%= link_to "Войти", signin_path %></li>
                <% else %>
                  <li><%= link_to  current_user.name, profile_path %></li>
                  <li>
                    <%= link_to "Корзина", mycart_path %>
                  </li>
                  <li><span class="badge"><%= Cart.where(:uid=>current_user.id).length %></span></li>
                  <li><%= link_to "Оплатить", checkout_path%></li>
                  <li><%= link_to "Мои заказы", orders_path%></li>
                  <li><%= link_to "Выйти", signout_path, method: "delete" %></li>
                <% end %>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
</div>